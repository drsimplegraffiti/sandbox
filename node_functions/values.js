var myName = 'joe';
var age = '45';
var history = 'killer';

function summarizeUsers(userName, userAge, userHistory) {
    return (userName, userAge, userHistory)
}

const result = summarizeUsers(myName, age, history)
console.log(result);