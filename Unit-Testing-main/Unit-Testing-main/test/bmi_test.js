const { assert } = require('chai');
const mocha = require('mocha');
const Bmi = require('../models/bmi');

//Describe test
describe('Body Mass Index', function() {

    //create test using it block
    it('should display Bmi models', function() {
        // assert or predict result or possible result
        assert()
    })
})