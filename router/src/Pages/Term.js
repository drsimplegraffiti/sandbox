import styles from '../styles/button.module.css'

const Term = () => {
    return (
        <div id="term" className={styles.profile_page}>
            <h1>Term of Use</h1>
        </div>
    );
}

export default Term;